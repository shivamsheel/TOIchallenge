<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Sign-up form</title>
  <link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet" integrity="sha384-wvfXpqpZZVQGK6TAh5PVlGOfQNHSoD2xbE+QkPxCAFlNEevoEH3Sl0sibVcOQVnN" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/3.5.2/animate.css" />
  <link rel="stylesheet" href="../public/css/style.css">
</head>
<body>
  <div class="container">
    <h1 class="brand"><span>Welcome User </span> Sign-up</h1>
    <div class="wrapper animated bounceInLeft">
      <div class="company-info">
        <h3>Election Commission Of India</h3>
        <ul>
          <li><i class="fa fa-road"></i>Election Commission Of India</li>
          <li><i class="fa fa-phone"></i> 011-25264609</li>
          <li><i class="fa fa-envelope"></i>EC@gmail.com</li>
        </ul>
      </div>
      <div class="contact">
        <h3>Email Us</h3>
        <form method="POST" action="/">
          <p>
            <label>First Name</label>
            <input type="text" name="firstname" required value="<%= oldInput.firstname %>">
            <% if (error.firstname) { %>
              <%= error.firstname.msg %>
            <% } %>
          </p>
          <p>
            <label>Aadhar Number</label>
            <input type="text" name="AdharNumber" required value="<%= oldInput.AdharNumber %>">
            <% if (error.AdharNumber) { %>
              <%= error.AdharNumber.msg %>
            <% } %>
          </p>
          <p>
            <label>Email Address</label>
            <input type="email" name="email" required value="<%= oldInput.email %>">
            <% if (error.email) { %>
              <%= error.email.msg %>
            <% } %>
          </p>
          <p>
            <label>Phone Number</label>
            <input type="number" name="phone" required value="<%= oldInput.phone %>">
            <% if (error.phone) { %>
              <%= error.phone.msg %>
            <% } %>
          </p>
          <p>
            <label>Voter Id</label>
            <input type="text" name="VoterID" required value="<%= oldInput.VoterID %>">
            <% if (error.VoterID) { %>
              <%= error.VoterID.msg %>
            <% } %>
          </p>
          <p>
          <label>Password</label>
            <input type="password" id="password" name="password" required value="<%= oldInput.password %>"> 
            <% if (error.password) { %>
              <%= error.password.msg %>
            <% } %>
          </p>
         
          <p class="full">
            <button type="submit">Submit</button>
          </p>
          <a class="login-btn" href="/login">Login</a>
          <a class="login-btn" href="/support"> Need Help?</a>
        </form>
      </div>
    </div>
  </div>
</body>
</html>




